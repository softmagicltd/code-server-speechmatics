# code-server Railway Deployment
# Copy this to .env and set your password.

# Option 1: Plain text password (simplest)
PASSWORD=changeme

# Option 2: Pre-hashed password (more secure, password never stored in plain text)
# Generate with: docker exec <container> set-password your-password
# HASHED_PASSWORD=$argon2id$v=19$m=65536,t=3,p=4$...

# If both are set, HASHED_PASSWORD takes priority.
